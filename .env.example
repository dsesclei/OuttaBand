# .env.example — copy to .env and fill in

# required — telegram bot creds + destination chat id (integer)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=123456789

# required — meteora price pair address (base58)
METEORA_PAIR_ADDRESS=your_pool_pair_address_here

# optional — scheduler + logic
# check interval minutes (default 15)
CHECK_EVERY_MINUTES=15
# per-(band,side) cooldown minutes (default 60)
COOLDOWN_MINUTES=60
# optional — seed/override band ranges on startup (format: low-high)
# if omitted, app ensures a/b/c exist as 0-100
# examples:
# BAND_A=201.90-203.10
# BAND_B=199.80-205.20
# BAND_C=193.00-212.00
